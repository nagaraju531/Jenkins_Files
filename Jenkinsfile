pipeline {
    agent {
    node {
      label 'linux'
    }
  }
    stages {
        stage('CheckOut the Code') {
            steps {
                // Get some code from a GitHub repository
                echo "Hello World"
           }
        }
    }
}
